# Руководство администратора

**Простое руководство для преподавателя по работе с системой документации**

---

## 🟢 Зеленый — хорошо, 🔴 Красный крестик — плохо

После внесения изменений в файлы и их сохранения (коммита), GitHub автоматически запускает процесс сборки сайта.

### Как проверить статус сборки:

1. Открывается репозиторий на GitHub
2. Переход во вкладку **"Actions"** (вверху страницы)
3. Проверка последнего запуска:

   - ✅ **Зеленая галочка** = всё работает, сайт обновился успешно
   - ❌ **Красный крестик** = что-то пошло не так, сайт не обновился

### Что делать, если появился красный крестик:

**⚠️ НЕ ПЫТАТЬСЯ ЧИНИТЬ САМОСТОЯТЕЛЬНО!**

1. **Ничего не трогать** — не вносить новые изменения
2. **Обратиться за помощью:**
   - Связаться со старостой следующего потока
   - Обратиться к технически подкованному студенту
   - Описать, что было изменено перед появлением ошибки

**Важно:** Технические проблемы решаются специалистами. Задача преподавателя — только редактировать текст.

---

## ✏️ Редактирование только текста

### ⚠️ ВАЖНО: Что можно править, а что нельзя

#### ✅ **МОЖНО править:**
- Весь текст после блока с тремя дефисами (`---`)
- Заголовки, списки, параграфы
- Примеры кода (если они есть в тексте)

#### ❌ **НЕЛЬЗЯ трогать:**
- **Блок между тремя дефисами в начале файла** (это системная информация)
- Названия файлов
- Структуру папок

### Пример правильного файла:

```markdown
---
id: lab7
title: Лабораторная работа №7
slug: /labs/computer-organization/labs-sem7/lab7
---

# Лабораторная работа №7

[Здесь начинается текст, который МОЖНО редактировать]

## Цель работы

Изучение порта JTAG UART...

[Весь остальной текст можно править свободно]
```

**⚠️ КРИТИЧЕСКИ ВАЖНО:**

- **НЕ УДАЛЯТЬ** дефисы (`---`) в начале и конце блока
- **НЕ ДОБАВЛЯТЬ** лишние пробелы в строках между дефисами
- **НЕ ИЗМЕНЯТЬ** содержимое между дефисами (строки с `id:`, `title:`, `slug:`)

Если этот блок был случайно изменен — сайт не соберется и появится красный крестик.

### Как редактировать через веб-интерфейс GitHub:

1. Открывается нужный файл (например, `website/docs/computer-organization/labs-sem7/lab7.md`)
2. Нажимается иконка **карандаша** (✏️) в правом верхнем углу
3. **Прокручивается вниз** мимо блока с дефисами
4. Редактируется только текст ниже этого блока
5. Нажимается **"Commit changes"** внизу страницы

---

## 💾 Данные вечны

### Материалы в безопасности

**Даже если сайт сломался или перестал обновляться, все методички остаются в репозитории.**

### Где хранятся файлы:

Все файлы с методичками находятся в папке:
```
website/docs/
```

### Как скачать файл напрямую:

1. Открывается нужный файл на GitHub (например, `website/docs/computer-organization/labs-sem7/lab7.md`)
2. Нажимается кнопка **"Raw"** (вверху справа)
3. Файл открывается в браузере как обычный текст
4. Сохраняется через меню браузера (Ctrl+S или Cmd+S)

### Альтернативный способ:

1. Открывается папка с файлами на GitHub
2. Нажимается на нужный файл
3. В правом верхнем углу нажимается **"Download"** (если доступно)
4. Или копируется содержимое вручную

**Вывод:** Даже в худшем случае (сайт полностью сломался) все данные остаются доступными в репозитории GitHub. Их можно скачать, скопировать или восстановить в любой момент.

---

## 📞 Что делать при проблемах

Если возникли проблемы:

1. **Красный крестик в Actions** → обратиться за помощью к технически подкованному студенту или старосте
2. **Вопросы по редактированию** → обратиться к старосте или студенту, знакомому с системой
3. **Нужно добавить новый файл** → лучше обратиться за помощью

**Важно:** Система создана для удобства, но технические вопросы лучше решать с помощью специалистов.

---

## 📝 Краткая памятка

1. ✅ **Зеленая галочка** = всё работает
2. ❌ **Красный крестик** = ничего не трогать, обратиться за помощью
3. ✏️ **Редактировать только текст** после блока с `---`
4. 💾 **Данные всегда доступны** в папке `website/docs/`

---

*Последнее обновление: 2025*
